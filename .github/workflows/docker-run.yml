name: Run Docker

on:
  workflow_dispatch:

#  pull_request:
#    branches:
#      - 'master'

jobs:
  build-docker:
    runs-on: self-hosted

    steps:
      - uses: actions/checkout@v2
    
      - name: Build the stack
        run: docker-compose up -d
